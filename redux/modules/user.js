// imports

// actions

// action creators

// API actions

// intiial state

// reducer

const initalState = {
    isLoggedIn: false
}

// reducer functions

const reducer = (state = initalState, action) => {
    switch(action.type) {
        default:
            return state;
    }
}

// exports

// reducer export

export default reducer;